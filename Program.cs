using labWork3;

// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");
Student st = new Student();
st = new Student();

st.Id = 43;
Console.WriteLine(st.Id);

//ternanry operator in c#
/*bool isLogedIn = true;
string message = isLogedIn ? "Logged in" : "Other message";

Student st = new Student();
st.id = 0;
Student st2 = new Student() { id = 0 };*/ // o can initialize the public fileds like this  I

/*private interface _id;

public int Id {
    get { return _id; }
    set { _id = value; }

    //or with arrow function
    get => return _id; 
    set => _id = value; 
*/
}

